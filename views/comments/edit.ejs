<%- include("../partials/header") %>
<div class="container">
    <div class="">
        <h1 style="text-align: center;">Edit</h1>
        <div style="width: 30%; margin: 20px auto; padding: 30px; border: 2px solid rgba(52, 52, 71,0.5);" >
            <form action="/campgrounds/<%= campground_id %>/comments/<%= comment._id %>?_method=put" method="POST">
                <div class="form-group">
                  <input type="text" class="form-control" name="comment[text]" value="<%= comment.text %>">
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary btn-block">Comment</button>
                </div>
            </form>
            <a class="btn btn-success btn-sm" href="/campgrounds/<%=campground_id%>">Go back</a>
        </div>
    </div>
</div>

<%- include("../partials/footer") %>